import { CircularProgress, Stack } from '@mui/material'

const CenterSpinner = () => {
  return (
    <Stack alignItems="center">
      <CircularProgress />
    </Stack>
  )
}

export default CenterSpinner
